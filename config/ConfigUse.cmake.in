##############################################################################
# @file  ConfigUse.cmake.in
# @brief CMake package configuration use file.
#
# @note The &lt;Package&gt;Use.cmake file is automatically generated by BASIS
#       from the template file ConfigUse.cmake.in which is part of this
#       source package.
#
# If this CMake module was already included before, it will only reset the
# BASIS settings for a new invocation of the function basis_project_impl(),
# for example, inside a module of a project.
#
# Copyright (c) 2011 University of Pennsylvania. All rights reserved.
# See https://www.rad.upenn.edu/sbia/software/license.html or COPYING file.
#
# Contact: SBIA Group <sbia-software at uphs.upenn.edu>
##############################################################################

# ----------------------------------------------------------------------------
# prefix used for variable names
set (NS "@PROJECT_NAME@_")

# ----------------------------------------------------------------------------
# Always reset the settings when this file is included!
include ("${${NS}MODULE_PATH}/BasisTools.cmake")
include ("${${NS}MODULE_PATH}/BasisSettings.cmake" NO_POLICY_SCOPE)

# ----------------------------------------------------------------------------
# include guard
if (__${NS}USE_FILE_INCLUDED)
  return ()
else ()
  set (__${NS}USE_FILE_INCLUDED TRUE)
endif ()

# ----------------------------------------------------------------------------
# BASIS utilities

# include directories
basis_include_directories ("${${NS}INCLUDE_DIR}")

# ----------------------------------------------------------------------------
# import build targets - has to be done AFTER including BasisProject.cmake!
if (NOT NO_${NS}IMPORTS)
  include ("${${NS}EXPORTS_FILE}")
  include ("${${NS}CUSTOM_EXPORTS_FILE}")
endif ()
